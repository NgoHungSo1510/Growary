# 🐉 V2.0 — Character Hunt Overhaul: "Hệ Thống Mở Khóa Nhân Vật"
## Plan Chi Tiết — Dành Cho Dev Tiếp Nhận

> **Version**: 2.0 (Bản chi tiết cho handoff)
> **Ngày lập plan**: 2026-09-05
> **Trạng thái**: Chờ triển khai
> **Phạm vi**: Backend + Admin Panel + Mobile App
> **Ước lượng**: ~24–31h
> **Người viết plan**: AI review từ codebase thực tế

---

## SO SÁNH V1 vs V2

| Khía cạnh | V1 (Hiện tại) | V2 (Mới) |
|-----------|---------------|----------|
| EXP → Damage | Tự động trừ HP khi hoàn thành task | Tích "Điểm Công" → User chủ động vào đập khóa |
| Tương tác | Chỉ xem animation damage thụ động | Mini-game 10s nhân damage x2. Hỗ trợ Hoàn Trả (Refund) sát thương dư |
| Nhân vật Visual | MaterialIcon + màu nền | Ảnh nhân vật bị làm mờ (Blur) + Ổ Khóa do admin upload |
| Mức Tấn Công | Tự động đánh toàn bộ điểm | Tự chọn mức đánh: 25% / 50% / 75% / ALL-IN (100%) |
| Chia thưởng | Ai tham gia cũng nhận thưởng cố định | Bảng Xếp Hạng: Top 1 (100%), Top 2 (75%), Top 3 (50%), Top 4+ (25%) |
| Phạt Tích Trữ | Điểm tích vô hạn | Mất 50% Điểm Công khi kết thúc một Bộ Sưu Tập |
| Admin Timeline | Phải kéo tay tìm boss | Auto-scroll tới nhân vật hiện tại |

---

## CODEBASE HIỆN TẠI — ĐỌC TRƯỚC KHI CODE

### Cấu trúc thư mục

```
Growary/
├── backend/src/
│   ├── models/          ← Mongoose schemas (TypeScript)
│   ├── controllers/     ← Request handlers
│   ├── routes/          ← Express routers
│   ├── services/        ← Business logic tách riêng
│   ├── jobs/            ← Cron jobs (node-cron)
│   ├── scripts/         ← Migration & seed scripts (chạy độc lập)
│   ├── middleware/      ← auth.ts (authMiddleware, adminMiddleware)
│   └── constants.ts     ← Hằng số (STREAK_MIN_TASKS, BOSS_HEAL_AMOUNT, ...)
├── admin/src/pages/     ← React admin pages
└── mobile/src/
    ├── screens/         ← React Native screens
    └── components/      ← Shared components
```

### Files Boss hiện tại (cần sửa/thêm)

| File | Vai trò |
|------|---------|
| `backend/src/models/BossEvent.ts` | Schema nhân vật bị khóa |
| `backend/src/models/BossRecord.ts` | Tracking đóng góp của từng user |
| `backend/src/controllers/eventController.ts` | Chỉ có 2 hàm: getActiveBoss, animateBossDamage |
| `backend/src/routes/events.ts` | Chỉ 2 routes: GET /boss/active, POST /boss/animate |
| `backend/src/services/bossService.ts` | distributeBossRewards(), checkAndActivateBosses() |
| `backend/src/controllers/planController.ts` | Dòng 289-307: logic tự trừ HP boss khi hoàn thành task |
| `backend/src/jobs/streak.ts` | startStreakCronJob(), startBossSchedulerJob() |
| `backend/src/routes/admin.ts` | File lớn 815 dòng — Boss CRUD đang nằm ở đây |
| `backend/src/scripts/seedBosses.ts` | Seed 15 boss cũ (sẽ thay thế) |
| `mobile/src/screens/BossEventScreen.tsx` | Screen boss hiện tại (sẽ redesign hoàn toàn) |
| `admin/src/pages/BossManagementPage.tsx` | Trang quản lý boss (sẽ mở rộng form) |
| `admin/src/pages/Events.tsx` | Tab Boss ở admin (sẽ thêm Collection section) |
| `backend/src/models/index.ts` | Export tất cả models — cần thêm 2 models mới |

### Logic V1 trong planController.ts cần THAY THẾ (dòng 289-307)

```typescript
// HIỆN TẠI (V1) — TỰ ĐỘNG TRỪ HP — CẦN XÓA BỎ:
const activeBoss = await BossEvent.findOne({ status: 'active' });
if (activeBoss) {
    activeBoss.currentHp = Math.max(0, activeBoss.currentHp - finalXpReward);
    if (activeBoss.currentHp === 0) activeBoss.status = 'completed';
    await activeBoss.save();  // <-- XÓA DÒNG NÀY

    if (activeBoss.status === 'completed') {
        distributeBossRewards(activeBoss._id.toString()).catch(console.error);
    }

    let userRecord = await BossRecord.findOne({ eventId: activeBoss._id, userId: req.userId });
    userRecord.totalDamageDealt += finalXpReward;    // <-- THAY ĐỔI
    userRecord.pendingDamageAnimation += finalXpReward; // <-- THAY ĐỔI
    await userRecord.save();
}
```

---

## PHASE 1 — DATABASE MODELS

**Ước lượng: 1.5-2h**

### 1.1 [MODIFY] backend/src/models/BossEvent.ts

Thêm vào INTERFACE IBossEvent (sau `iconName?: string;`):

```typescript
avatarImageUrl?: string;        // URL ảnh đại diện nhân vật (admin upload, client tự blur)
loreTitle?: string;             // Tên câu chuyện VD: "Bí Ẩn Doraemon"
loreContent?: string;           // Nội dung cốt truyện (có thể dài)
collectionId?: mongoose.Types.ObjectId; // Thuộc BossCollection nào
isLimited?: boolean;            // Nhân vật sự kiện giới hạn (default: true)
miniGameQuestions?: {
    question: string;
    options: string[];           // Mảng 4 đáp án [A, B, C, D]
    correctIndex: number;        // 0-indexed (0=A, 1=B, 2=C, 3=D)
}[];
```

Thêm vào SCHEMA (bên trong new Schema, sau `iconName: { ... }`):

```typescript
avatarImageUrl: { type: String, default: null },
loreTitle: { type: String, trim: true, default: null },
loreContent: { type: String, trim: true, default: null },
collectionId: { type: Schema.Types.ObjectId, ref: 'BossCollection', default: null },
isLimited: { type: Boolean, default: true },
miniGameQuestions: [
    {
        question: { type: String, required: true },
        options: [{ type: String }],
        correctIndex: { type: Number, required: true },
    }
],
```

### 1.2 [MODIFY] backend/src/models/BossRecord.ts

Thêm vào INTERFACE IBossRecord (sau `pendingDamageAnimation: number;`):

```typescript
attackPoints: number;           // Điểm Công tích lũy từ hoàn thành task
hasUnlockedStory: boolean;      // Đã mở khóa câu chuyện nhân vật chưa
lastMiniGameType?: string;      // 'tap' | 'quiz' | 'reflex' (log for debugging)
lastMiniGameResult?: string;    // 'critical' | 'normal' (log for debugging)
```

Thêm vào SCHEMA (bên trong new Schema, sau `pendingDamageAnimation: { ... }`):

```typescript
attackPoints: { type: Number, default: 0 },
hasUnlockedStory: { type: Boolean, default: false },
lastMiniGameType: { type: String, default: null },
lastMiniGameResult: { type: String, default: null },
```

### 1.3 [NEW] backend/src/models/BossCollection.ts

Tạo file mới hoàn toàn:

```typescript
import mongoose, { Document, Schema } from 'mongoose';

export interface IBossCollection extends Document {
    title: string;              // "Bộ Tháng 9 — Bạn Bè Doraemon"
    description: string;
    month: number;              // 9
    year: number;               // 2026
    iconEmoji: string;          // "🍂"
    completionStory: string;    // Truyện tổng kết, hiện khi user sưu tập đủ 100%
    bonusTickets: number;       // Số vé gacha thưởng khi hoàn thành bộ
    isActive: boolean;
    createdAt: Date;
    updatedAt: Date;
}

const BossCollectionSchema = new Schema<IBossCollection>(
    {
        title: { type: String, required: true, trim: true },
        description: { type: String, trim: true },
        month: { type: Number, required: true, min: 1, max: 12 },
        year: { type: Number, required: true },
        iconEmoji: { type: String, default: '📚' },
        completionStory: { type: String, trim: true, default: '' },
        bonusTickets: { type: Number, default: 0 },
        isActive: { type: Boolean, default: true },
    },
    { timestamps: true }
);

export const BossCollection = mongoose.model<IBossCollection>('BossCollection', BossCollectionSchema);
```

### 1.4 [NEW] backend/src/models/UserBossCollection.ts

Tạo file mới hoàn toàn:

```typescript
import mongoose, { Document, Schema } from 'mongoose';

export interface IUserBossCollection extends Document {
    userId: mongoose.Types.ObjectId;
    collectionId: mongoose.Types.ObjectId;
    unlockedBosses: mongoose.Types.ObjectId[]; // Danh sách _id boss đã mở khóa
    isCompleted: boolean;
    completedAt?: Date;
    bonusTicketsClaimed: boolean;
    createdAt: Date;
    updatedAt: Date;
}

const UserBossCollectionSchema = new Schema<IUserBossCollection>(
    {
        userId: { type: Schema.Types.ObjectId, ref: 'User', required: true },
        collectionId: { type: Schema.Types.ObjectId, ref: 'BossCollection', required: true },
        unlockedBosses: [{ type: Schema.Types.ObjectId, ref: 'BossEvent' }],
        isCompleted: { type: Boolean, default: false },
        completedAt: { type: Date, default: null },
        bonusTicketsClaimed: { type: Boolean, default: false },
    },
    { timestamps: true }
);

// Unique: Mỗi user chỉ có 1 entry per collection
UserBossCollectionSchema.index({ userId: 1, collectionId: 1 }, { unique: true });

export const UserBossCollection = mongoose.model<IUserBossCollection>('UserBossCollection', UserBossCollectionSchema);
```

### 1.5 [MODIFY] backend/src/models/index.ts

Thêm 2 dòng export vào cuối file:

```typescript
export { BossCollection, IBossCollection } from './BossCollection';
export { UserBossCollection, IUserBossCollection } from './UserBossCollection';
```

---

## PHASE 2 — BACKEND API

**Ước lượng: 4-5h**

### 2.1 [MODIFY] backend/src/controllers/planController.ts

TÌM dòng 289-307 và THAY THẾ toàn bộ khối boss logic:

```typescript
// SAU (V2) — Tích Điểm Công, KHÔNG trừ HP trực tiếp:
const activeBoss = await BossEvent.findOne({ status: 'active' });
if (activeBoss) {
    let userRecord = await BossRecord.findOne({ eventId: activeBoss._id, userId: req.userId });
    if (!userRecord) {
        userRecord = new BossRecord({
            eventId: activeBoss._id,
            userId: req.userId,
            totalDamageDealt: 0,
            accumulatedCoins: 0,
            pendingDamageAnimation: 0,
            attackPoints: 0,
            hasUnlockedStory: false,
        });
    }
    // V2: Tích Điểm Công thay vì trừ HP
    userRecord.attackPoints += finalXpReward;
    // accumulatedCoins vẫn tích (Rương riêng, nhận 100% khi boss chết)
    userRecord.accumulatedCoins += Math.floor(finalCoinReward / 2);
    // pendingDamageAnimation không còn dùng trong V2
    await userRecord.save();
}
```

> QUAN TRỌNG: Xóa bỏ hoàn toàn `activeBoss.currentHp -= finalXpReward` và `activeBoss.save()` khỏi planController. HP boss chỉ bị trừ khi user chủ động "Phá Khóa".

### 2.2 [NEW] Thêm 4 hàm vào backend/src/controllers/eventController.ts

File hiện tại chỉ có 44 dòng với 2 hàm. Thêm import và 4 hàm mới:

```typescript
// Thêm import ở đầu file:
import { BossCollection, UserBossCollection } from '../models';
import { User } from '../models';
import { checkAndActivateBosses } from '../services/bossService';
```

#### Hàm attackBoss

```typescript
export const attackBoss = async (req: AuthRequest, res: Response): Promise<void> => {
    try {
        const { miniGameType, miniGameResult, usePercentage, questionIndex, answerIndex } = req.body;

        // Validate usePercentage
        const validPercentages = [0.25, 0.5, 0.75, 1.0];
        if (!validPercentages.includes(usePercentage)) {
            res.status(400).json({ error: 'Invalid usePercentage. Must be 0.25, 0.5, 0.75, or 1.0' });
            return;
        }

        await checkAndActivateBosses();
        const activeBoss = await BossEvent.findOne({ status: 'active' });
        if (!activeBoss) { res.status(404).json({ error: 'No active character found' }); return; }
        if (activeBoss.currentHp <= 0) { res.status(400).json({ error: 'Character already unlocked' }); return; }

        let userRecord = await BossRecord.findOne({ eventId: activeBoss._id, userId: req.userId });
        if (!userRecord || userRecord.attackPoints <= 0) {
            res.status(400).json({ error: 'No attack points available' }); return;
        }

        // Verify quiz result trên server (bảo mật, client không tự quyết được)
        let actualMiniGameResult = miniGameResult;
        if (miniGameType === 'quiz' && questionIndex !== undefined && answerIndex !== undefined) {
            const q = activeBoss.miniGameQuestions?.[questionIndex];
            actualMiniGameResult = (q && q.correctIndex === answerIndex) ? 'critical' : 'normal';
        }

        // Tính damage
        const pointsToUse = Math.floor(userRecord.attackPoints * usePercentage);
        if (pointsToUse <= 0) { res.status(400).json({ error: 'Not enough attack points' }); return; }

        const multiplier = actualMiniGameResult === 'critical' ? 2 : 1;
        const potentialDamage = pointsToUse * multiplier;

        let actualDamage = potentialDamage;
        let refundPoints = 0;

        // Cơ chế Refund: Nếu damage vượt quá HP còn lại
        if (potentialDamage > activeBoss.currentHp) {
            actualDamage = activeBoss.currentHp;
            const overkillDamage = potentialDamage - actualDamage;
            // Hoàn trả điểm chưa dùng hết
            refundPoints = Math.floor(overkillDamage / multiplier);
        }

        // Cập nhật Boss HP
        activeBoss.currentHp -= actualDamage;
        if (activeBoss.currentHp <= 0) {
            activeBoss.currentHp = 0;
            activeBoss.status = 'completed';
        }
        await activeBoss.save();

        // Cập nhật BossRecord user
        userRecord.attackPoints = (userRecord.attackPoints - pointsToUse) + refundPoints;
        userRecord.totalDamageDealt += actualDamage;
        userRecord.lastMiniGameType = miniGameType;
        userRecord.lastMiniGameResult = actualMiniGameResult;
        await userRecord.save();

        // Nếu boss chết → chia thưởng
        if (activeBoss.status === 'completed') {
            const { distributeBossRewards } = await import('../services/bossService');
            distributeBossRewards(activeBoss._id.toString()).catch(console.error);
        }

        res.json({
            success: true,
            actualDamage,
            refundPoints,
            isCritical: actualMiniGameResult === 'critical',
            bossCurrentHp: activeBoss.currentHp,
            bossStatus: activeBoss.status,
            userAttackPoints: userRecord.attackPoints,
            userTotalDamage: userRecord.totalDamageDealt,
        });
    } catch (error) {
        console.error('Failed to attack boss:', error);
        res.status(500).json({ error: 'Internal server error' });
    }
};
```

#### Hàm getBossQuiz

```typescript
export const getBossQuiz = async (req: AuthRequest, res: Response): Promise<void> => {
    try {
        const activeBoss = await BossEvent.findOne({ status: 'active' });
        if (!activeBoss || !activeBoss.miniGameQuestions || activeBoss.miniGameQuestions.length === 0) {
            res.status(404).json({ error: 'No quiz questions available' });
            return;
        }
        const randomIndex = Math.floor(Math.random() * activeBoss.miniGameQuestions.length);
        const question = activeBoss.miniGameQuestions[randomIndex];
        // KHÔNG trả về correctIndex để tránh client cheat
        res.json({
            questionIndex: randomIndex,
            question: question.question,
            options: question.options,
        });
    } catch (error) {
        console.error('Failed to get boss quiz:', error);
        res.status(500).json({ error: 'Internal server error' });
    }
};
```

#### Hàm getBossCollections

```typescript
export const getBossCollections = async (req: AuthRequest, res: Response): Promise<void> => {
    try {
        const collections = await BossCollection.find({ isActive: true }).sort({ year: 1, month: 1 });

        const result = await Promise.all(collections.map(async (col) => {
            const bosses = await BossEvent.find({ collectionId: col._id }, '_id title avatarImageUrl status');
            let userCol = await UserBossCollection.findOne({ userId: req.userId, collectionId: col._id });
            if (!userCol) {
                userCol = await UserBossCollection.create({
                    userId: req.userId, collectionId: col._id,
                    unlockedBosses: [], isCompleted: false, bonusTicketsClaimed: false
                });
            }

            return {
                collection: col,
                bosses: bosses.map(b => ({
                    _id: b._id,
                    title: b.title,
                    avatarImageUrl: b.avatarImageUrl,
                    status: b.status,
                    isUnlocked: userCol!.unlockedBosses.some(id => id.toString() === b._id.toString()),
                })),
                userProgress: {
                    unlockedCount: userCol.unlockedBosses.length,
                    totalCount: bosses.length,
                    isCompleted: userCol.isCompleted,
                    bonusTicketsClaimed: userCol.bonusTicketsClaimed,
                },
            };
        }));

        res.json({ collections: result });
    } catch (error) {
        console.error('Failed to get boss collections:', error);
        res.status(500).json({ error: 'Internal server error' });
    }
};
```

#### Hàm claimCollectionReward

```typescript
export const claimCollectionReward = async (req: AuthRequest, res: Response): Promise<void> => {
    try {
        const { id } = req.params;
        const collection = await BossCollection.findById(id);
        if (!collection) { res.status(404).json({ error: 'Collection not found' }); return; }

        const userCol = await UserBossCollection.findOne({ userId: req.userId, collectionId: id });
        if (!userCol || !userCol.isCompleted) {
            res.status(400).json({ error: 'Collection not completed yet' }); return;
        }
        if (userCol.bonusTicketsClaimed) {
            res.status(400).json({ error: 'Reward already claimed' }); return;
        }

        const user = await User.findById(req.userId);
        if (!user) { res.status(404).json({ error: 'User not found' }); return; }
        user.gachaTickets += collection.bonusTickets;
        await user.save();

        userCol.bonusTicketsClaimed = true;
        await userCol.save();

        res.json({ success: true, ticketsGranted: collection.bonusTickets });
    } catch (error) {
        console.error('Failed to claim collection reward:', error);
        res.status(500).json({ error: 'Internal server error' });
    }
};
```

### 2.3 [MODIFY] backend/src/routes/events.ts

Thay toàn bộ nội dung file:

```typescript
import { Router } from 'express';
import { authMiddleware } from '../middleware/auth';
import * as eventController from '../controllers/eventController';

const router = Router();

router.get('/boss/active', authMiddleware, eventController.getActiveBoss);
router.post('/boss/animate', authMiddleware, eventController.animateBossDamage);
// V2 NEW:
router.post('/boss/attack', authMiddleware, eventController.attackBoss);
router.get('/boss/quiz', authMiddleware, eventController.getBossQuiz);
router.get('/boss/collections', authMiddleware, eventController.getBossCollections);
router.post('/boss/collections/:id/claim', authMiddleware, eventController.claimCollectionReward);

export default router;
```

### 2.4 [MODIFY] backend/src/services/bossService.ts

Thay thế hoàn toàn hàm `distributeBossRewards`. Giữ nguyên `checkAndActivateBosses`:

```typescript
import { User, BossEvent, BossRecord, Journal, Voucher } from '../models';
import { BossCollection, UserBossCollection } from '../models';
import { processLevelUp } from './levelService';
import { v4 as uuidv4 } from 'uuid';
import { VOUCHER_EXPIRY_DAYS, getStartOfDay } from '../constants';

export const distributeBossRewards = async (bossId: string) => {
    try {
        // Guard chống race condition
        const boss = await BossEvent.findOneAndUpdate(
            { _id: bossId, isRewardDistributed: false },
            { isRewardDistributed: true },
            { new: true }
        );
        if (!boss) {
            console.log(`Boss ${bossId}: rewards already distributed, skipping.`);
            return;
        }
        await boss.populate('rewardItems');
        console.log(`Distributing rewards for: ${boss.title}`);

        // Lấy tất cả records có đóng góp, sort theo totalDamageDealt DESC (Top ranking)
        const records = await BossRecord.find({ eventId: bossId, totalDamageDealt: { $gt: 0 } })
            .sort({ totalDamageDealt: -1 });

        // Hệ số thưởng: Top 1=100%, Top 2=75%, Top 3=50%, Top 4+=25%
        const getRankMultiplier = (index: number): number => {
            if (index === 0) return 1.0;
            if (index === 1) return 0.75;
            if (index === 2) return 0.5;
            return 0.25;
        };

        for (let i = 0; i < records.length; i++) {
            const record = records[i];
            const user = await User.findById(record.userId);
            if (!user) continue;

            const multiplier = getRankMultiplier(i);

            // Thưởng gốc x hệ số Top
            const bonusCoins = Math.floor(boss.baseRewardCoins * multiplier);
            const bonusXp = Math.floor(boss.baseRewardXp * multiplier);
            const bonusTickets = Math.floor((boss.gachaTickets || 0) * multiplier);

            // Rương cá nhân (accumulatedCoins từ quest hàng ngày) — luôn 100%, không giảm theo Top
            const personalChest = record.accumulatedCoins;

            user.coins += bonusCoins + personalChest;
            user.currentPoints += bonusXp;
            user.totalPointsEarned += bonusXp;
            if (bonusTickets > 0) user.gachaTickets += bonusTickets;

            // Đánh dấu đã mở khóa câu chuyện
            record.hasUnlockedStory = true;
            await record.save();

            // Distribute item rewards (tất cả user có đóng góp đều nhận)
            if (boss.rewardItems && boss.rewardItems.length > 0) {
                const expiresAt = new Date();
                expiresAt.setDate(expiresAt.getDate() + VOUCHER_EXPIRY_DAYS);
                for (const rw of boss.rewardItems as any) {
                    await Voucher.create({
                        user: user._id,
                        reward: rw._id,
                        code: `BOSS-${uuidv4().slice(0, 8).toUpperCase()}`,
                        pointCostSnapshot: 0,
                        rewardTitleSnapshot: rw.title,
                        expiresAt,
                    });
                    if (rw.stock !== undefined) {
                        rw.stock -= 1;
                        if (rw.stock <= 0) rw.isActive = false;
                        await rw.save();
                    }
                }
            }

            if (bonusXp > 0) await processLevelUp(user, bonusXp);
            await user.save();

            // Cập nhật UserBossCollection — đánh dấu boss đã unlock
            if (boss.collectionId) {
                const userCol = await UserBossCollection.findOneAndUpdate(
                    { userId: user._id, collectionId: boss.collectionId },
                    { $addToSet: { unlockedBosses: boss._id } },
                    { upsert: true, new: true }
                );
                if (userCol) {
                    const totalInCollection = await BossEvent.countDocuments({ collectionId: boss.collectionId });
                    if (userCol.unlockedBosses.length >= totalInCollection && !userCol.isCompleted) {
                        userCol.isCompleted = true;
                        userCol.completedAt = new Date();
                        await userCol.save();
                        console.log(`User ${user.username} completed collection!`);
                    }
                }
            }

            // Journal auto-log
            const today = getStartOfDay();
            const rankLabel = i === 0 ? 'Top 1' : i === 1 ? 'Top 2' : i === 2 ? 'Top 3' : `Top ${i + 1}`;
            await Journal.findOneAndUpdate(
                { user: user._id, date: today },
                {
                    $push: {
                        autoLogs: {
                            taskId: boss._id,
                            taskTitle: `Mo khoa nhan vat: ${boss.title} (${rankLabel} — ${bonusCoins} Xu, ${bonusXp} XP)`,
                            completedAt: new Date(),
                        },
                    },
                    $inc: { totalTasksCompleted: 1, totalPointsEarned: bonusXp },
                },
                { upsert: true }
            );

            console.log(`${rankLabel} ${user.username}: ${bonusCoins + personalChest} Coins, ${bonusXp} XP, ${bonusTickets} Tickets`);
        }
    } catch (error) {
        console.error('Error distributing boss rewards:', error);
    }
};

// Giữ nguyên hàm checkAndActivateBosses — KHÔNG THAY ĐỔI
export const checkAndActivateBosses = async () => {
    // ... (giữ nguyên code cũ)
};
```

### 2.5 [MODIFY] backend/src/routes/admin.ts

Thêm các routes sau vào cuối file, TRƯỚC `export default router`:

```typescript
// Thêm imports ở đầu file admin.ts (cùng với các imports khác):
import { BossCollection } from '../models/BossCollection';
import { UserBossCollection } from '../models/UserBossCollection';

// ==================== BOSS COLLECTIONS ====================

router.get('/boss-collections', async (_req: AuthRequest, res: Response): Promise<void> => {
    try {
        const collections = await BossCollection.find().sort({ year: 1, month: 1 });
        const result = await Promise.all(collections.map(async (col) => {
            const bossCount = await BossEvent.countDocuments({ collectionId: col._id });
            return { ...col.toObject(), bossCount };
        }));
        res.json({ collections: result });
    } catch (error) {
        res.status(500).json({ error: 'Internal server error' });
    }
});

router.post('/boss-collections', async (req: AuthRequest, res: Response): Promise<void> => {
    try {
        const { title, description, month, year, iconEmoji, completionStory, bonusTickets } = req.body;
        const collection = await BossCollection.create({ title, description, month, year, iconEmoji, completionStory, bonusTickets });
        res.json({ collection });
    } catch (error) {
        res.status(500).json({ error: 'Internal server error' });
    }
});

router.put('/boss-collections/:id', async (req: AuthRequest, res: Response): Promise<void> => {
    try {
        const collection = await BossCollection.findByIdAndUpdate(req.params.id, req.body, { new: true });
        if (!collection) { res.status(404).json({ error: 'Not found' }); return; }
        res.json({ collection });
    } catch (error) {
        res.status(500).json({ error: 'Internal server error' });
    }
});

router.delete('/boss-collections/:id', async (req: AuthRequest, res: Response): Promise<void> => {
    try {
        await BossCollection.findByIdAndDelete(req.params.id);
        await BossEvent.updateMany({ collectionId: req.params.id }, { $unset: { collectionId: 1 } });
        res.json({ success: true });
    } catch (error) {
        res.status(500).json({ error: 'Internal server error' });
    }
});

// Upload avatar: nhận URL đã upload lên Cloudinary
router.post('/boss/:id/upload-avatar', async (req: AuthRequest, res: Response): Promise<void> => {
    try {
        const { avatarImageUrl } = req.body;
        const boss = await BossEvent.findByIdAndUpdate(req.params.id, { avatarImageUrl }, { new: true });
        if (!boss) { res.status(404).json({ error: 'Boss not found' }); return; }
        res.json({ boss });
    } catch (error) {
        res.status(500).json({ error: 'Internal server error' });
    }
});
```

---

## PHASE 3 — CRON JOBS

**Ước lượng: 1h**

### 3.1 [MODIFY] backend/src/jobs/streak.ts

Thêm function mới ở cuối file (giữ nguyên 2 function cũ):

```typescript
// Thêm imports ở đầu file:
import { BossCollection, BossRecord, BossEvent } from '../models';

export function startCollectionPenaltyJob() {
    // Chạy mỗi ngày lúc 01:00 VN = 18:00 UTC
    cron.schedule('0 18 * * *', async () => {
        console.log('Running collection penalty check...');
        try {
            const now = new Date();
            const activeCollections = await BossCollection.find({ isActive: true });

            for (const col of activeCollections) {
                // Boss cuối cùng trong collection (sort endTime giảm dần)
                const lastBoss = await BossEvent.findOne({ collectionId: col._id }).sort({ endTime: -1 });
                if (!lastBoss) continue;

                // Nếu boss cuối đã hết giờ → collection kết thúc → phạt 50%
                if (lastBoss.endTime <= now) {
                    console.log(`Collection "${col.title}" ended. Applying 50% penalty.`);

                    col.isActive = false;
                    await col.save();

                    // Lấy tất cả boss thuộc collection
                    const bossIds = await BossEvent.find({ collectionId: col._id }).distinct('_id');

                    // Trừ 50% attackPoints của TẤT CẢ user (không trùng)
                    // Dùng MongoDB aggregation pipeline update để chia nguyên
                    const penalizedUsers = new Set<string>();
                    const records = await BossRecord.find({ eventId: { $in: bossIds }, attackPoints: { $gt: 0 } });

                    for (const record of records) {
                        const uid = record.userId.toString();
                        if (penalizedUsers.has(uid)) continue;
                        penalizedUsers.add(uid);

                        await BossRecord.updateMany(
                            { userId: record.userId, attackPoints: { $gt: 0 } },
                            [{ $set: { attackPoints: { $floor: { $divide: ['$attackPoints', 2] } } } }]
                        );
                    }
                    console.log(`Penalized ${penalizedUsers.size} users.`);
                }
            }
        } catch (error) {
            console.error('Collection penalty error:', error);
        }
    });
    console.log('Collection penalty job scheduled (daily at 01:00 VN)');
}
```

### 3.2 [MODIFY] backend/src/server.ts

Tìm nơi gọi `startStreakCronJob()` và `startBossSchedulerJob()`, thêm dòng mới:

```typescript
import { startStreakCronJob, startBossSchedulerJob, startCollectionPenaltyJob } from './jobs/streak';

// (bên trong block khởi động)
startStreakCronJob();
startBossSchedulerJob();
startCollectionPenaltyJob(); // THÊM DÒNG NÀY
```

---

## PHASE 4 — ADMIN PANEL

**Ước lượng: 3-4h**

### 4.1 [MODIFY] admin/src/pages/BossManagementPage.tsx

#### Timeline Auto-Scroll

Thêm ref và useEffect vào component:

```typescript
const activeCardRef = useRef<HTMLDivElement>(null);

useEffect(() => {
    if (activeCardRef.current) {
        activeCardRef.current.scrollIntoView({
            behavior: 'smooth',
            inline: 'center',
            block: 'nearest',
        });
    }
}, [events]); // Trigger sau khi fetch events xong

// Trong JSX — gán ref cho card active:
<div ref={event.status === 'active' ? activeCardRef : undefined} ...>
```

#### Form tạo/sửa boss — Thêm section V2

Giữ nguyên toàn bộ fields cũ. Thêm 2 section mới ở phía dưới:

**Section 1: Nhân vật**

```jsx
<div className="form-section">
  <h4>Nhan Vat</h4>

  <label>
    <input type="checkbox"
      checked={formData.isLimited ?? true}
      onChange={e => setFormData({...formData, isLimited: e.target.checked})}
    />
    Nhan vat su kien gioi han (chi xuat hien 1 lan)
  </label>

  <div>
    <label>Anh dai dien nhan vat</label>
    {formData.avatarImageUrl && (
      <img src={formData.avatarImageUrl} alt="preview" style={{width: 100, height: 100, objectFit: 'cover'}} />
    )}
    <input type="text"
      placeholder="Paste URL anh"
      value={formData.avatarImageUrl || ''}
      onChange={e => setFormData({...formData, avatarImageUrl: e.target.value})}
    />
  </div>

  <input type="text"
    placeholder="Ten cau chuyen (VD: Bi An Doraemon)"
    value={formData.loreTitle || ''}
    onChange={e => setFormData({...formData, loreTitle: e.target.value})}
  />

  <textarea
    placeholder="Noi dung cot truyen nhan vat (hien khi khoa bi pha vo)"
    rows={4}
    value={formData.loreContent || ''}
    onChange={e => setFormData({...formData, loreContent: e.target.value})}
  />

  <label>Thuoc bo suu tap</label>
  <select
    value={formData.collectionId || ''}
    onChange={e => setFormData({...formData, collectionId: e.target.value})}
  >
    <option value="">-- Khong thuoc bo nao --</option>
    {collections.map(col => (
      <option key={col._id} value={col._id}>{col.iconEmoji} {col.title}</option>
    ))}
  </select>
</div>
```

**Section 2: Quiz Mini-game**

```jsx
<div className="form-section">
  <h4>Cau Hoi Mini-Game (Quick Quiz)</h4>
  <p style={{color: '#888', fontSize: 12}}>
    Neu khong co cau hoi → Mini-game chi dung Tap Speed hoac Reflex Hit
  </p>

  {(formData.miniGameQuestions || []).map((q, idx) => (
    <div key={idx}>
      <input
        type="text"
        placeholder={`Cau ${idx + 1}: Noi dung cau hoi`}
        value={q.question}
        onChange={e => updateQuestion(idx, 'question', e.target.value)}
      />
      {['A', 'B', 'C', 'D'].map((opt, optIdx) => (
        <div key={optIdx} style={{display: 'flex', gap: 8, alignItems: 'center'}}>
          <input type="radio" name={`correct-${idx}`}
            checked={q.correctIndex === optIdx}
            onChange={() => updateQuestion(idx, 'correctIndex', optIdx)}
          />
          <span>{opt}:</span>
          <input type="text" placeholder={`Dap an ${opt}`}
            value={q.options[optIdx] || ''}
            onChange={e => updateOption(idx, optIdx, e.target.value)}
          />
        </div>
      ))}
      <button onClick={() => removeQuestion(idx)}>Xoa cau nay</button>
    </div>
  ))}
  <button onClick={addQuestion}>+ Them cau hoi</button>
</div>
```

**Helper functions cần thêm:**

```typescript
const addQuestion = () => {
    setFormData(prev => ({
        ...prev,
        miniGameQuestions: [...(prev.miniGameQuestions || []), { question: '', options: ['', '', '', ''], correctIndex: 0 }]
    }));
};
const removeQuestion = (idx: number) => {
    setFormData(prev => ({ ...prev, miniGameQuestions: prev.miniGameQuestions?.filter((_, i) => i !== idx) }));
};
const updateQuestion = (idx: number, field: string, value: any) => {
    setFormData(prev => {
        const qs = [...(prev.miniGameQuestions || [])];
        qs[idx] = { ...qs[idx], [field]: value };
        return { ...prev, miniGameQuestions: qs };
    });
};
const updateOption = (qIdx: number, optIdx: number, value: string) => {
    setFormData(prev => {
        const qs = [...(prev.miniGameQuestions || [])];
        const opts = [...qs[qIdx].options];
        opts[optIdx] = value;
        qs[qIdx] = { ...qs[qIdx], options: opts };
        return { ...prev, miniGameQuestions: qs };
    });
};
```

#### Timeline card — badges mới

```jsx
{/* Thumbnail thay color box */}
{event.avatarImageUrl
    ? <img src={event.avatarImageUrl} style={{width:40, height:40, borderRadius:8, objectFit:'cover'}} />
    : <div style={{background: event.colorBg}}><i>{event.iconName}</i></div>
}
{/* Badges */}
{event.loreTitle && <span className="badge">Story</span>}
{event.miniGameQuestions?.length > 0 && <span className="badge">{event.miniGameQuestions.length} Quiz</span>}
{event.collectionId && <span className="badge">Collection</span>}
{event.isLimited && <span className="badge badge-red">Limited</span>}
```

### 4.2 [MODIFY] admin/src/pages/Events.tsx

Thêm section quản lý BossCollection bên dưới phần Timeline hiện có. Section này cần:

- State: `collections: IBossCollection[]`, fetch từ `GET /admin/boss-collections`
- Nút tạo mới mở modal
- Danh sách collection: tên, emoji, tháng/năm, bonusTickets, bossCount, isActive
- Nút Sửa / Xóa
- Modal tạo/sửa với fields: title, description, month, year, iconEmoji, completionStory (textarea), bonusTickets (number)

---

## PHASE 5 — MOBILE UI REDESIGN

**Ước lượng: 4-5h**

### 5.1 [MODIFY] mobile/src/screens/BossEventScreen.tsx

**Cập nhật interfaces:**

```typescript
interface ActiveBoss {
    _id: string;
    title: string;
    description: string;
    startTime: string;
    endTime: string;
    maxHp: number;
    currentHp: number;
    baseRewardCoins: number;
    baseRewardXp?: number;
    gachaTickets?: number;
    rewardItems?: any[];
    status: string;
    colorBg?: string;
    colorIcon?: string;
    iconName?: string;
    // V2 NEW:
    avatarImageUrl?: string;
    loreTitle?: string;
    loreContent?: string;
    isLimited?: boolean;
    miniGameQuestions?: Array<{question: string; options: string[]; correctIndex: number}>;
}

interface BossRecord {
    totalDamageDealt: number;
    accumulatedCoins: number;
    pendingDamageAnimation: number;
    // V2 NEW:
    attackPoints: number;
    hasUnlockedStory: boolean;
}
```

**State mới:**

```typescript
const [attackPercentage, setAttackPercentage] = useState<0.25 | 0.5 | 0.75 | 1.0>(1.0);
const [isMiniGameVisible, setIsMiniGameVisible] = useState(false);
const [miniGameType, setMiniGameType] = useState<'tap' | 'quiz' | 'reflex'>('tap');
const [quizData, setQuizData] = useState<{questionIndex: number; question: string; options: string[]} | null>(null);
const [isAttackResultVisible, setIsAttackResultVisible] = useState(false);
const [lastAttackResult, setLastAttackResult] = useState<{damage: number; refund: number; isCritical: boolean} | null>(null);
const [isStoryModalVisible, setIsStoryModalVisible] = useState(false);
const [collections, setCollections] = useState<any[]>([]);
```

**fetchData cập nhật:**

```typescript
const fetchData = async () => {
    const res = await apiService.get('/events/boss/active');
    if (res.activeBoss) {
        setBoss(res.activeBoss);
        setRecord(res.userRecord);
        setDisplayHp(res.activeBoss.currentHp); // V2: không cần logic pendingDamage cũ

        // Hiện StoryModal nếu đã unlock và chưa xem
        if (res.userRecord?.hasUnlockedStory && res.activeBoss?.currentHp === 0) {
            const key = `story_seen_${res.activeBoss._id}`;
            const seen = await AsyncStorage.getItem(key);
            if (!seen) {
                setIsStoryModalVisible(true);
                await AsyncStorage.setItem(key, 'true');
            }
        }
    }
    // Fetch collections
    const colRes = await apiService.get('/events/boss/collections');
    if (colRes.collections) setCollections(colRes.collections);
};
```

**Logic nhấn PHÁ KHÓA:**

```typescript
const handleAttackPress = async () => {
    if (!record || record.attackPoints <= 0) return;

    const hasQuiz = boss?.miniGameQuestions && boss.miniGameQuestions.length > 0;
    const options = hasQuiz ? ['tap', 'quiz', 'reflex'] : ['tap', 'reflex'];
    const selectedType = options[Math.floor(Math.random() * options.length)] as 'tap' | 'quiz' | 'reflex';
    setMiniGameType(selectedType);

    // Nếu quiz → fetch câu hỏi trước
    if (selectedType === 'quiz') {
        const qRes = await apiService.get('/events/boss/quiz');
        setQuizData(qRes);
    }

    setIsMiniGameVisible(true);
};
```

**Logic sau mini-game:**

```typescript
const handleMiniGameComplete = async (result: 'critical' | 'normal', extraData?: {questionIndex?: number; answerIndex?: number}) => {
    setIsMiniGameVisible(false);
    try {
        const body: any = { miniGameType, miniGameResult: result, usePercentage: attackPercentage };
        if (miniGameType === 'quiz' && extraData) {
            body.questionIndex = extraData.questionIndex;
            body.answerIndex = extraData.answerIndex;
            // Không gửi miniGameResult cho quiz — backend tự tính từ answerIndex
        }
        const res = await apiService.post('/events/boss/attack', body);
        setLastAttackResult({ damage: res.actualDamage, refund: res.refundPoints, isCritical: res.isCritical });
        setIsAttackResultVisible(true);
        await fetchData();
    } catch (error) {
        Alert.alert('Loi', 'Khong the tan cong. Thu lai.');
    }
};
```

**Layout JSX:**

```jsx
<ScrollView>
    {/* Banner Nhân vật */}
    <CharacterBanner boss={boss} displayHp={displayHp} />

    {/* Khu vực tấn công (chỉ khi boss chưa unlock) */}
    {boss && boss.currentHp > 0 && (
        <AttackSection
            record={record}
            selectedPercentage={attackPercentage}
            onSelectPercentage={setAttackPercentage}
            onAttackPress={handleAttackPress}
        />
    )}

    {/* Trạng thái đã mở khóa */}
    {boss && boss.currentHp <= 0 && (
        <TouchableOpacity onPress={() => setIsStoryModalVisible(true)}>
            <Text>Nhan vat da duoc mo khoa! Nhan vao anh de doc cot truyen.</Text>
        </TouchableOpacity>
    )}

    {/* Thông tin đóng góp */}
    {record && <ContributionInfo record={record} />}

    {/* Gallery Bộ sưu tập */}
    <CollectionGallery collections={collections} />
</ScrollView>
```

**CharacterBanner (gợi ý logic):**

```
- Nếu boss.avatarImageUrl + currentHp > 0:
    <Image source={{uri: boss.avatarImageUrl}} />
    <BlurView intensity={80} style={StyleSheet.absoluteFill} />    ← expo-blur
    <MaterialIcons name="lock" size={80} />                        ← Icon khóa to
- Nếu boss.avatarImageUrl + currentHp <= 0:
    <Image source={{uri: boss.avatarImageUrl}} />                   ← Ảnh rõ nét
- Nếu không có avatarImageUrl:
    Fallback MaterialIcons (giữ nguyên V1)
- Nếu isLimited && currentHp > 0:
    Text "Nhan vat su kien gioi han — Chi xuat hien 1 lan!"
- Progress bar HP + countdown thời gian
```

**AttackSection (gợi ý logic):**

```
- Text: "Dang co: {record.attackPoints} Diem Cong"
- 4 nút [25%] [50%] [75%] [MAX] với highlight khi selected
- Preview: "= {Math.floor(record.attackPoints * selectedPercentage)} Diem Cong"
- Button "PHA KHOA" — disabled nếu attackPoints = 0
- Nếu = 0: text "Hoan thanh nhiem vu de tich Diem Cong!"
```

### 5.2 [MODIFY] mobile/src/screens/EventScreen.tsx

Cập nhật card boss trong EventScreen:
- Nếu có `avatarImageUrl` → hiện thumbnail (blur nhẹ nếu HP > 0)
- Badge "Limited" nếu `isLimited === true`
- Badge "Collection" nếu thuộc bộ sưu tập

---

## PHASE 6 — MOBILE MINI-GAME & MODALS

**Ước lượng: 4-5h**

### 6.1 [NEW] mobile/src/components/MiniGameModal.tsx

Props:

```typescript
interface MiniGameModalProps {
    visible: boolean;
    type: 'tap' | 'quiz' | 'reflex';
    quizQuestion?: { questionIndex: number; question: string; options: string[] };
    onComplete: (result: 'critical' | 'normal', extraData?: {questionIndex?: number; answerIndex?: number}) => void;
    onSkip: () => void;
}
```

**Variant TAP SPEED — "Búa Tạ":**
- Thời gian: 5 giây countdown
- Điều kiện Critical: >= 20 lần nhấn
- UI: Nút lớn, counter "X / 20", progress bar 5s
- Logic: setInterval đếm ngược, khi hết gọi onComplete với result tùy tapCount

**Variant QUICK QUIZ — "Giải Mã":**
- Thời gian: 10 giây countdown
- Điều kiện Critical: Backend verify (không phải client!)
- UI: Text câu hỏi, 4 nút A/B/C/D, countdown 10s
- Logic: Khi chọn đáp án → gọi onComplete('normal', {questionIndex, answerIndex})
  (Backend sẽ override result dựa trên correctIndex)

**Variant REFLEX HIT — "Điểm Yếu":**
- Thời gian: 8 giây, 3 target
- Điều kiện Critical: Chạm đúng cả 3/3
- UI: Background tối, 3 vòng tròn sáng xuất hiện tuần tự tại random position
- Logic: hitCount tăng khi chạm đúng target hiện tại, kết thúc → onComplete(hitCount===3?'critical':'normal')

**Nút Skip (chung):**
- Text "Bo qua (Normal Hit)"
- Gọi onSkip() → caller gọi handleMiniGameComplete('normal')

### 6.2 [NEW] mobile/src/components/AttackResultModal.tsx

Props:

```typescript
interface AttackResultModalProps {
    visible: boolean;
    damage: number;
    refundPoints: number;
    isCritical: boolean;
    onClose: () => void;
}
```

UI:
- Text lớn: "CRITICAL HIT! x2" hoặc "Normal Hit"
- Damage number với animation từ 0 → damage
- Nếu refundPoints > 0: "Hoan tra {refundPoints} Diem Cong"
- Button "OK"

### 6.3 [NEW] mobile/src/components/StoryUnlockModal.tsx

Props:

```typescript
interface StoryUnlockModalProps {
    visible: boolean;
    boss: { title: string; avatarImageUrl?: string; loreTitle?: string; loreContent?: string };
    isInCollection: boolean;
    onClose: () => void;
}
```

UI:
- Full-screen modal, dark overlay
- Ảnh nhân vật rõ nét full-size (fallback icon)
- Hiệu ứng glow
- Text: loreTitle
- ScrollView: loreContent
- Nếu isInCollection: "Da them vao Bo Suu Tap!"
- Button "Dong"

**Trigger trong BossEventScreen fetchData:**

```typescript
if (res.userRecord?.hasUnlockedStory && res.activeBoss?.currentHp === 0) {
    const key = `story_seen_${res.activeBoss._id}`;
    const seen = await AsyncStorage.getItem(key);
    if (!seen) {
        setIsStoryModalVisible(true);
        await AsyncStorage.setItem(key, 'true');
    }
}
```

---

## PHASE 7 — MIGRATION & SEED

**Ước lượng: 1-1.5h**

### 7.1 [NEW] backend/src/scripts/migrateBossV2.ts

```typescript
import mongoose from 'mongoose';
import dotenv from 'dotenv';

dotenv.config();
const MONGO_URI = process.env.MONGODB_URI || 'mongodb://localhost:27017/growary';

const migrate = async () => {
    await mongoose.connect(MONGO_URI);
    console.log('Connected to MongoDB');

    const eventsDeleted = await mongoose.connection.db.collection('bossevents').deleteMany({});
    const recordsDeleted = await mongoose.connection.db.collection('bossrecords').deleteMany({});

    console.log(`Deleted ${eventsDeleted.deletedCount} BossEvents`);
    console.log(`Deleted ${recordsDeleted.deletedCount} BossRecords`);
    console.log('Migration complete. Run seedBossV2.ts next.');
    process.exit(0);
};

migrate().catch(err => { console.error(err); process.exit(1); });
```

Chạy: `npx ts-node backend/src/scripts/migrateBossV2.ts`

### 7.2 [NEW] backend/src/scripts/seedBossV2.ts

```typescript
import mongoose from 'mongoose';
import dotenv from 'dotenv';
import { BossCollection } from '../models/BossCollection';
import { BossEvent } from '../models/BossEvent';

dotenv.config();
const MONGO_URI = process.env.MONGODB_URI || 'mongodb://localhost:27017/growary';

const seed = async () => {
    await mongoose.connect(MONGO_URI);

    // Tạo 2 Collections
    const col1 = await BossCollection.create({
        title: 'Bo Thang 9 — Ban Be Doraemon', description: 'Kham pha the gioi cua nhung nhan vat huyen thoai',
        month: 9, year: 2026, iconEmoji: '🌙',
        completionStory: 'Ban da suu tap du tat ca ban be Doraemon! Cau chuyen hoan chinh...',
        bonusTickets: 5, isActive: true,
    });

    const col2 = await BossCollection.create({
        title: 'Bo Thang 10 — Le Hoi Ma Quai', description: 'Mua Halloween day bi an va phieu luu',
        month: 10, year: 2026, iconEmoji: '🎃',
        completionStory: 'Ban da chinh phuc tat ca thu thach Halloween!...',
        bonusTickets: 8, isActive: true,
    });

    const now = new Date();
    const weekOffset = (w: number) => {
        const d = new Date(now);
        d.setDate(d.getDate() + w * 7);
        return d;
    };
    const addDays = (d: Date, days: number) => { const r = new Date(d); r.setDate(r.getDate() + days); return r; };

    const bosses = [
        // Collection 1 (Tháng 9)
        {
            title: 'Doraemon Luoi Bieng', collectionId: col1._id, status: 'active',
            description: 'Phien ban luoi nhac cua Doraemon!',
            maxHp: 5000, baseRewardCoins: 200, baseRewardXp: 100, gachaTickets: 2,
            startTime: weekOffset(0), endTime: addDays(weekOffset(0), 6),
            loreTitle: 'Bi An Chiec Tui Than Ky', loreContent: 'Ngay xua, co mot chu meo may xanh...', isLimited: true,
            miniGameQuestions: [
                { question: 'Doraemon den tu the ky nao?', options: ['21', '22', '23', '24'], correctIndex: 1 },
                { question: 'Bao boi noi tieng nhat?', options: ['Co May Thoi Gian', 'Cua Than Ky', 'Tui Troi Dat', 'Chong Chong Tre'], correctIndex: 2 },
            ],
        },
        {
            title: 'Nobita Tri Hoan', collectionId: col1._id, status: 'upcoming',
            description: 'Nobita luon de bai tap den phut chot!',
            maxHp: 6000, baseRewardCoins: 250, baseRewardXp: 120, gachaTickets: 2,
            startTime: weekOffset(1), endTime: addDays(weekOffset(1), 6),
            loreTitle: 'Hanh Trinh Thay Doi', loreContent: 'Nobita quyet dinh khong luoi bieng nua...', isLimited: true,
            miniGameQuestions: [{ question: 'Nobita thuong bi diem may?', options: ['5', '0', '3', '1'], correctIndex: 1 }],
        },
        {
            title: 'Shizuka Ngu Nuong', collectionId: col1._id, status: 'upcoming',
            description: 'Shizuka diu dang nhung toi nay khong chiu day!',
            maxHp: 4500, baseRewardCoins: 180, baseRewardXp: 90, gachaTickets: 1,
            startTime: weekOffset(2), endTime: addDays(weekOffset(2), 6),
            loreTitle: 'Buoi Sang Cua Nang Tien', loreContent: 'Shizuka thich day som va tap dan...', isLimited: true,
            miniGameQuestions: [], // Chỉ Tap + Reflex
        },
        // Collection 2 (Tháng 10)
        {
            title: 'Bong Ma Tu Ti', collectionId: col2._id, status: 'upcoming',
            description: 'Thuc thực den toi sinh ra tu su tu ti!',
            maxHp: 5500, baseRewardCoins: 220, baseRewardXp: 110, gachaTickets: 2,
            startTime: weekOffset(4), endTime: addDays(weekOffset(4), 6),
            loreTitle: 'Chinh Phuc Bong Toi Noi Tam', loreContent: 'Trong dem Halloween, bong ma tu ti xuat hien...', isLimited: true,
            miniGameQuestions: [],
        },
        {
            title: 'Quai Thu Man Hinh', collectionId: col2._id, status: 'upcoming',
            description: 'Sinh vat song trong dien thoai!',
            maxHp: 5000, baseRewardCoins: 200, baseRewardXp: 100, gachaTickets: 2,
            startTime: weekOffset(5), endTime: addDays(weekOffset(5), 6),
            loreTitle: 'Thoat Khoi The Gioi Ao', loreContent: 'Moi gio dan mat vao man hinh...', isLimited: true,
            miniGameQuestions: [
                { question: 'Bao nhieu gio/ngay dung dien thoai la lanh manh?', options: ['1-2h', '3-4h', '5-6h', 'Khong gioi han'], correctIndex: 0 },
            ],
        },
        {
            title: 'Thuc The Song Ao', collectionId: col2._id, status: 'upcoming',
            description: 'Ke lang thang giua mang xa hoi!',
            maxHp: 7000, baseRewardCoins: 300, baseRewardXp: 150, gachaTickets: 3,
            startTime: weekOffset(6), endTime: addDays(weekOffset(6), 6),
            loreTitle: 'Tro Ve Thuc Tai', loreContent: 'Cuoc song thuc luon dep hon man hinh...', isLimited: true,
            miniGameQuestions: [
                { question: 'Mang xa hoi nao pho bien nhat VN 2024?', options: ['Facebook', 'TikTok', 'Instagram', 'Twitter'], correctIndex: 1 },
            ],
        },
    ];

    for (const boss of bosses) {
        await BossEvent.create({ ...boss, currentHp: boss.maxHp });
        console.log(`Seeded: ${boss.title}`);
    }

    console.log('Seed complete! 2 collections, 6 characters.');
    process.exit(0);
};

seed().catch(err => { console.error(err); process.exit(1); });
```

Chạy: `npx ts-node backend/src/scripts/seedBossV2.ts`

---

## PHASE 8 — TESTING CHECKLIST

**Ước lượng: 2-3h**

### Backend Tests

| Test Case | Input | Expected |
|-----------|-------|----------|
| Tích Điểm Công | Hoàn thành task khi có boss active | userRecord.attackPoints tăng bằng finalXpReward |
| HP không tự trừ | Hoàn thành task | activeBoss.currentHp KHÔNG thay đổi |
| Tấn công normal 100% | usePercentage:1.0, miniGameResult:'normal' | damage=attackPoints, refund=0 |
| Tấn công critical 50% | usePercentage:0.5, miniGameResult:'critical' | damage=50%x2, attackPoints còn 50% |
| Overkill Refund | Boss còn 100 HP, user 200 điểm critical (400 damage) | actualDamage=100, refund=floor(300/2)=150 |
| Leaderboard rewards | 4 users: 500, 300, 200, 100 damage | Coins: 100%, 75%, 50%, 25% |
| accumulatedCoins full | User top4 có 100 accumulatedCoins | Nhận đủ 100 coins từ rương riêng |
| Quiz verify đúng | answerIndex == correctIndex | isCritical: true |
| Quiz verify sai | answerIndex != correctIndex | isCritical: false |
| Collection complete | Unlock hết 3 boss trong bộ | userCol.isCompleted=true |
| Claim reward | isCompleted=true, bonusTicketsClaimed=false | gachaTickets tăng, bonusTicketsClaimed=true |
| Claim reward 2nd | bonusTicketsClaimed=true | Error 400 |
| Penalty cron | Collection endTime qua | attackPoints = floor(attackPoints/2) |

### Mobile Tests

| Test Case | Action | Expected |
|-----------|--------|----------|
| Ảnh blur | Boss có avatarImageUrl + HP > 0 | Ảnh bị blur + icon khóa |
| Ảnh rõ | Boss HP = 0 | Ảnh rõ nét, không blur, không khóa |
| Điểm Công = 0 | Không có điểm | Nút "PHA KHOA" disabled |
| Chọn 25% | 100 điểm, chọn 25% | Preview hiện "= 25 Diem Cong" |
| Mini-game Tap critical | Nhấn >= 20 lần/5s | result = critical |
| Mini-game Tap fail | Nhấn < 20 lần | result = normal |
| StoryModal trigger | Boss HP=0, chưa xem | Modal hiện tự động 1 lần |
| StoryModal 2nd visit | Đã xem, vào lại | Modal KHÔNG hiện nữa |
| Collection gallery | 2/3 boss unlocked | 2 ảnh, 1 ô khóa |
| Collection complete | 3/3 unlocked | Viền vàng, nút "Nhan thuong" |

---

## TỔNG HỢP FILES CẦN THAY ĐỔI

### Backend (14 files)

| Action | File | Thay đổi |
|--------|------|----------|
| MODIFY | `models/BossEvent.ts` | +6 fields |
| MODIFY | `models/BossRecord.ts` | +4 fields |
| NEW | `models/BossCollection.ts` | Model bộ sưu tập |
| NEW | `models/UserBossCollection.ts` | Model tracking per user |
| MODIFY | `models/index.ts` | Export 2 models mới |
| MODIFY | `controllers/eventController.ts` | +4 functions mới |
| MODIFY | `controllers/planController.ts` | Dòng 289-307: đổi logic |
| MODIFY | `services/bossService.ts` | Rewrite distributeBossRewards |
| MODIFY | `routes/events.ts` | +4 routes |
| MODIFY | `routes/admin.ts` | +5 routes Collection CRUD |
| MODIFY | `jobs/streak.ts` | +1 function penalty job |
| MODIFY | `server.ts` | Gọi startCollectionPenaltyJob |
| NEW | `scripts/migrateBossV2.ts` | Migration script |
| NEW | `scripts/seedBossV2.ts` | Seed 2 collections + 6 characters |

### Admin (2 files)

| Action | File | Thay đổi |
|--------|------|----------|
| MODIFY | `pages/BossManagementPage.tsx` | Auto-scroll + form mở rộng |
| MODIFY | `pages/Events.tsx` | Section Collection CRUD |

### Mobile (6 files)

| Action | File | Thay đổi |
|--------|------|----------|
| MODIFY | `screens/BossEventScreen.tsx` | Redesign hoàn toàn |
| MODIFY | `screens/EventScreen.tsx` | Boss card V2 |
| NEW | `components/MiniGameModal.tsx` | 3 mini-game variants |
| NEW | `components/AttackResultModal.tsx` | Damage result |
| NEW | `components/StoryUnlockModal.tsx` | Story popup |

---

## LƯU Ý QUAN TRỌNG

### Breaking Changes
- Schema đổi → Data cũ KHÔNG compatible → BẮT BUỘC chạy `migrateBossV2.ts` trước
- `planController.ts` không còn tự trừ HP boss

### Backward Compatibility
- `avatarImageUrl` rỗng → fallback MaterialIcon (V1 UI)
- `miniGameQuestions` rỗng → chỉ dùng Tap / Reflex
- `collectionId` null → boss không thuộc bộ, OK

### Giữ nguyên từ V1
- Streak break → boss hồi máu `BOSS_HEAL_AMOUNT = 50` (trong `streak.ts`)
- `checkAndActivateBosses()` logic chuyển upcoming → active — KHÔNG thay đổi
- `animateBossDamage` — giữ lại để không break, V2 không còn dùng

### BlurView trong React Native
```
Dùng expo-blur (có sẵn trong Expo):
import { BlurView } from 'expo-blur';

<ImageBackground source={{uri: boss.avatarImageUrl}}>
    <BlurView intensity={80} style={StyleSheet.absoluteFill} tint="dark" />
    <MaterialIcons name="lock" size={80} color="white" />
</ImageBackground>
```

---

## THỨ TỰ TRIỂN KHAI

| Phase | Nội dung | Ước lượng |
|-------|----------|-----------|
| 1 | DB Models (4 files) + index.ts | 1.5-2h |
| 2 | planController + eventController + routes + bossService | 4-5h |
| 3 | Admin routes Collection CRUD | 1h |
| 4 | Cron job penalty + server.ts | 1h |
| 5 | Admin Panel UI | 3-4h |
| 6 | Mobile BossEventScreen redesign | 4-5h |
| 7 | Mobile Mini-game Modal (3 variants) | 3-4h |
| 8 | Mobile AttackResult + StoryUnlock Modals | 1.5-2h |
| 9 | Migration + Seed scripts | 1-1.5h |
| 10 | Testing | 2-3h |
| Tong | 22 files thay doi | ~24-31h |
